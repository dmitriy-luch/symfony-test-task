Page:
  actAs: { Timestampable: ~ }
  columns:
    title: { type: string(255), notnull: true }
    url: { type: varchar(255), unique: true }
    meta: { type: varchar(255) }
    content: { type: string }